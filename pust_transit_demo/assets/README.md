# Assets

The MVP logo is drawn with Flutter widgets and Material icons, so no external runtime image assets are required. Add verified PUST branding files here before production use and declare them in `pubspec.yaml`.
